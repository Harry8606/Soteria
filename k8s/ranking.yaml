apiVersion: apps/v1
kind: Deployment
metadata:
  name: Soteria-ranking
spec:
  replicas: 3
  selector:
    matchLabels:
      app: Soteria-ranking
  template:
    metadata:
      labels:
        app: Soteria-ranking
    spec:
      containers:
      - name: ranking-service
        image: Soteria/ranking:latest
        resources:
          requests:
            cpu: "8"
            memory: "32Gi"
            nvidia.com/gpu: "1"
          limits:
            cpu: "16"
            memory: "64Gi"
            nvidia.com/gpu: "1"
        ports:
        - containerPort: 8001
        env:
        - name: SERVICE_ROLE
          value: "ranking"
---
apiVersion: v1
kind: Service
metadata:
  name: Soteria-ranking-svc
spec:
  selector:
    app: Soteria-ranking
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
